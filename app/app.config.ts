export default defineAppConfig({
    ui: {
        main: {
            // base: 'min-h-[calc(100vh-var(--ui-header-height))]'
        },
        colors: {
            primary: 'emerald',
            secondary: 'pink'
        }
    },
    theme: {
        radius: 0.25,
        blackAsPrimary: false
    }
})
